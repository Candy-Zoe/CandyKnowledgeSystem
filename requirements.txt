PySide6>=6.6.0
tiktoken>=0.7.0
numpy>=1.24.0

# Optional PDF fallback parsers. The built-in PDF parser runs first.
pymupdf>=1.24.0
pdfplumber>=0.11.0

# Optional text encoding detection used by some parsers.
chardet>=5.0.0
